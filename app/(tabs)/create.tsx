import { ThemedText } from '@/components/themed-text';
import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';

const CreateScreen = () => {
  return (
    <SafeAreaView>
      <ThemedText>
        Ai create look page under construction...
      </ThemedText>
    </SafeAreaView>
  );
};

export default CreateScreen;